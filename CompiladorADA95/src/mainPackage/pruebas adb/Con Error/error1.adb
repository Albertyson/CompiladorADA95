procedure Hola is

    x,a, : integer ;

begin 
    
    x := 5;
    get (a);
    put (x);
    
    while a > 5 loop
        Pointer := 3;
        exit when a = b;
    end ;

    for i in 0..5 loop
        s := i;
    end loop;



    case X is
        when a    => Item := 1;
        when b    => Item := 2;
        when c    => Item := 3;
        when d    => Item := 4;
        when others => Item := 5;
    end case;





end Hola;