procedure pruebaSuma is
   	a,b,c: Integer;
begin
   	a := 5 + 3;
      b := a + 1;
      c := a + b;
      a := c + 3 + b;
      -- ASASASAS

      a := 5 + 3;
      b := a + 1;
      c := a + b;
      a := c + 3 + b;

      a := 5 + 3;
      b := a + 1;
      c := a + b;
      a := c + 3 + b;

end pruebaSuma;